<h1>New Act</h1>

<%= form_for @act do |f| %>
  <div class="form-group">
    <%= f.label :act %>
    <%= f.text_area :act, class: 'form-control' %>
    <span class="help-block">One sentence description of the random act of kindness you performed, ex: "Paid for the coffee of the person in the line behind me"</span>
  </div>
  <div class="form-group">
    <%= f.label :comment %> (optional)
    <%= f.text_area :comment, class: 'form-control' %>
    <span class="help-block">A few lines on how your random act of kindness came about, what was the response, how it made you feel, etc.
  </div>
  <%= f.submit 'Submit' %>
<% end %>